# PRD — VIP 카카오 애드핏 배선

> 2026-07-30 · playcast-blog (Virtual in Playing)

## 배경

watch 페이지에 애드핏 슬롯이 이미 배선돼 있었으나 유닛이 `DAN-VIP-REPLACE-ME`
플레이스홀더라 광고 요청 자체가 나가지 않았다(미노출). 홈·장르 페이지는 슬롯 0개.
애드핏 콘솔에서 유닛 6개를 발급받아 실제 노출로 전환한다.

## 유닛 → 자리 매핑

| 유닛 코드 | 규격 | 자리 | 노출 조건 |
|---|---|---|---|
| `DAN-pwo3tYrXlLQvJa3g` | 728x90 | watch 플레이어 아래 | ≥768px (PC·태블릿) |
| `DAN-sdH3bZFqWCgZlTJ1` | 320x100 | watch 플레이어 아래 | <768px (모바일) |
| `DAN-Zr9h4NXLISsFH6ww` | 320x100 | 앵커 — 전 페이지 하단 고정 | <768px |
| `DAN-C0L9Acg4VFJet5sC` | 300x250 | watch 본문 하단("이어서 볼 방송" 앞) | **≥783px** |
| `DAN-xiFmPLYjqxZyyYNd` | 300x250 | 홈 "전체 영상" 타이틀 앞 | 전 뷰포트 |
| `DAN-1jVfOQopXk0gBu42` | 320x100 | 장르 페이지 그리드 위 | 전 뷰포트 |

## 설계 규칙

### 1. 페이지당 애드핏 2개 상한 — 뷰포트별로 갈라서 지킨다

`BaseLayout`에 앵커가 들어가므로 watch 페이지는 최대 3개가 될 수 있다.
뷰포트로 갈라서 **어느 폭에서도 동시 노출은 2개**가 되게 한다.

- **<768px**: 상단 320x100 + 앵커 320x100 (하단 300x250은 783px 게이트로 꺼짐)
- **≥783px**: 상단 728x90 + 하단 300x250 (앵커는 768px 이상에서 꺼짐)
- 768~782px 구간: 상단 728x90 만 (앵커·하단 둘 다 꺼짐) — 의도된 공백

### 2. 하단 300x250 게이트 = 783px

앵커(320x100)가 `max-width:767px`에서 화면 바닥에 고정되므로, 하단 300x250을 그 아래 폭에서
노출하면 독자가 글 끝에 멈춰 있는 동안 두 광고가 세로로 인접한다.
→ 하단 슬롯은 783px 이상에서만. (뭉게 블로그에서 실측으로 정한 임계값과 동일)

### 3. 슬롯은 항상 렌더 — `display:none`은 요청조차 막는다

`:not(:has(iframe)){display:none}` 패턴은 영구 데드락이다(iframe이 생겨야 보여주는데
숨겨져 있으면 광고 요청이 안 나가서 iframe이 영원히 안 생긴다).
→ 슬롯은 계속 렌더하고 **여백(margin)만** `:has(iframe)`으로 조건부 적용.
뷰포트 전용 슬롯(PC 728x90 / 모바일 320x100)을 미디어쿼리로 숨기는 것은 의도된 동작이다.

### 4. ⚠️ 하단 고정배너에 닫기(×) 버튼 금지

애드핏 심사 반려 사유: "광고의 오클릭을 유발시킬 수 있는 매체는 광고게재가 허용되지 않습니다
— 광고 닫기 버튼이 설치되어 있어 보류" (2026-07-27, life-revenue-blog 실사례).
어떤 형태로도 재도입 금지.

### 5. `ba.min.js`는 페이지당 1회만 로드

BaseLayout body 끝에서 한 번 로드한다(슬롯마다 로드하면 중복). async라 실행 시점엔
서버 렌더된 모든 `ins`가 DOM에 있다.

### 6. 광고를 넣지 않는 곳

- `embed/[...slug]` — iframe 임베드 전용·noindex. BaseLayout을 쓰지 않으므로 앵커도 안 붙는다.
- `HostStage` 안/플레이어 위 — 영상형 페이지의 첫 화면을 밀어낸다.
- `about` — 트래픽 없음.

## 선행 확인 사항

- **AdSense 자동광고가 전역으로 이미 돌고 있다**(`BaseLayout.astro`, ca-pub-9400779918671270).
  애드핏 슬롯 옆에 자동광고가 꽂히면 광고 인접이 생길 수 있다. 라이브 확인 후
  자동광고를 끄거나 애드핏 자리를 줄이는 판단이 필요하다. (이번 작업 범위 밖 — 사용자 판단)
- **유닛 코드는 콘솔 스크린샷(이미지)에서 옮겨 적었다.** `l`/`I`/`1`, `0`/`O` 오독 시
  조용히 미노출된다. 배포 전 콘솔 텍스트와 대조 필요.

## 검증 방법

- 자동화로는 **DOM 배치까지만** 검증한다. 애드핏은 headless·headed 브라우저 모두 서빙하지
  않으므로(ba.min.js·ct2.html은 받아오지만 슬롯을 처리하지 않음) 실제 노출은 사람이 확인한다.
- 뷰포트별 게이트(768/783px 경계)는 자동 검증 대상 — 각 폭에서 보이는 `ins`의 유닛·개수를 센다.
