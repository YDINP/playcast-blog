# VIP(playcast) 리포터 캐릭터 초안

> 작성 2026-07-29 · 상태: **초안(미확정)** · 기준 캐릭터: 앵커 **로지(Rosie)** `public/host/rosie.png`

## 0. 설계 원칙 — "같은 방송국 크루"로 묶는다

로지의 결을 잇는다는 건 머리색을 비슷하게 가는 게 아니라, **같은 화가가 같은 규칙으로 그린
같은 방송국 소속**으로 읽히게 만드는 것이다. 그래서 아래로 층을 나눈다.

| 층 | 내용 | 공통/개별 |
|---|---|---|
| 그림체 | 밝은 애니 일러스트, 얇고 깔끔한 선, **밝은** 소프트 셀셰이딩(그림자 가볍게), 큰 눈 + 하이라이트 2겹, 흰 배경 | **공통(고정)** |
| 유니폼 | 크리스프한 **화이트 드레스 셔츠** + 목에 시그니처 컬러 1점 | **공통(고정)** |
| 연령대 | 22세 전후로 통일 | **공통(고정)** |
| 프레이밍 | 앵커=바스트업(데스크+콘덴서 마이크) / **리포터=허벅지까지 반신 정면 직립**(무선 핸드헬드 마이크) | 포지션별 |
| 시그니처 컬러 | 머리색 또는 넥웨어 | **개별** |
| 담당 주제·톤 | 코너 성격 | **개별** |

시그니처 컬러는 사이트 팔레트(`--host:#57e6c3`, `--live:#ff3d54`)와 충돌하지 않게 색상환에서 벌린다.
다크 UI(#0f0f11) 위에서 명도 대비가 확보되는지 확인할 것.

### 운용 방식 — 씬 단위 배치

한 영상에 여러 캐릭터가 동시에 등장하는 구성은 아직 쓰지 않는다. **뉴스처럼 "리포터1 → 씬1"
단위로 배치**한다(한 영상에 1인 기본). 그래서 담당 역할이 배타적이어야 할 필요가 없고,
캐릭터 구분은 **담당 주제와 화법 톤**으로만 준다.

| 인물 | 시그니처 | 머리 | 눈 | 프레이밍 | 담당 주제·톤 |
|---|---|---|---|---|---|
| 로지 (기존) | 민트 `#57e6c3` | 민트/아쿠아 롱 스트레이트 | 틸 | 바스트업(스튜디오) | **메인 앵커** — 진행·정리, 밝고 명랑 |
| 미아 (초안) | 라벤더 `#b28cf0` | 라벤더-바이올렛 하프업 포니테일 | 앰버 골드 | **반신 스탠딩** | 신작 첫인상·이벤트 속보, 텐션 높고 리액션 큼 |
| 레이 (초안) | 슬레이트 블루 `#5b7fa6` (넥타이) | 애쉬 슬레이트 `#5c6b7a` | 블루 그레이 | **반신 스탠딩(현장)** | 현장 취재·심층 리포트, 차분하고 자신감 있는 톤 |
| 카이 (초안) | 코발트 블루 (넥타이) | 딥 네이비 `#2a3a6b` | 앰버 | **반신 스탠딩** | 데이터·분석, 낮은 톤·결론부터 |

카이만 머리색이 시그니처가 아니라 **넥타이가 시그니처**다. 남성 캐릭터에 판타지 헤어를 쓰면
위화감이 커서(원색 코발트 헤어 시안이 그랬다) 머리는 저채도로 내리고 색은 목에서 준다.

---

## 1. 미아 (Mia) ♀ — 신작·속보

**한 줄**: 신작·이벤트를 두고 "이거 진짜 미쳤어요"를 먼저 외치는 에너지 담당.

- **머리**: 라벤더-바이올렛(`#b28cf0`), 하프업 포니테일 + 잔땋기 한 가닥, 사이드 스웹트 뱅.
  로지의 "길고 차분한 스트레이트"와 대비되는 **움직이는 실루엣**.
- **눈**: 따뜻한 앰버 골드. 로지의 차가운 틸과 온도 대비.
- **얼굴**: 로지보다 살짝 둥근 소녀상, 활짝 웃는 입(로지는 닫힌 미소) → 밝기 한 단계 위.
- **복장**: 화이트 셔츠 + **라벤더 네커치프**(로지의 타이 자리를 스카프로 변주) +
  베이지 프레스 재킷을 **정식 착용하고 소매 롤업**(어깨에 걸치지 않음 — 팔 실루엣이 명확해야
  리그 파츠에서 팔과 재킷소매를 한 덩어리로 처리할 수 있다) + 차콜 무릎길이 플리츠 스커트 +
  얇은 벨트 + 가슴 포켓 프레스 ID 카드.
  - 하의를 레이(슬랙스)와 갈라 스커트로 두면 나란히 세웠을 때 실루엣이 구분된다.
- **소품**: **무선 핸드헬드 스틱 마이크**(케이블 없음, 가슴 높이).
- **프레이밍**: 레이와 동일 규격 — **머리~허벅지 반신, 정면 직립**(어깨·골반 수평,
  고개 기울임 없음, 양발 균등, 마이크 든 팔 외 나머지 팔은 자연스럽게 내림).
- **말투**: 감탄사 많고 문장이 빠름. 로지가 정리하면 미아가 흥분한다.
- **상태**: 초안 `public/host/draft/mia.png` (= mia-v2-worn, 반신 정면 직립)

## 2. 레이 (Rei) ♀ — 현장 취재

**한 줄**: 현장에 서서 차분하게 사실을 짚는 취재 담당. 크루 중 가장 어른스러운 톤.

- **머리**: 애쉬 슬레이트 블루그레이(`#5c6b7a`) **단일색**(투톤·이너하이라이트 금지),
  **어깨선 길이의 레이어드 중단발** — 끝이 어깨에 닿고 일부 가닥은 가슴 앞으로 떨어진다.
  소프트 사이드 스웹트 뱅 + 얼굴 감싸는 잔가닥, 가볍게 흐트러진 텍스처.
- **눈**: 블루 그레이. 머리와 톤 통일.
- **얼굴**: 자신감 있는 반쯤 웃는 입매, 정면 응시, 눈꼬리가 살짝 올라간 눈매.
  **걸크러쉬 감각은 헤어·포즈·표정으로 주고, 눈 크기·채색은 크루 규격을 따른다.**
- **복장**: 화이트 셔츠(칼라 단정히 닫음) 소매 롤업 + **슬레이트 블루 슬림 타이** +
  다크 차콜 슬림 슬랙스 + 얇은 벨트 + 랜야드 프레스 ID.
  ⚠**랜야드는 오른쪽 가슴에 짧게 걸어 타이를 가리지 않는다** — 타이가 매듭부터 끝까지
  보여야 시그니처 컬러가 성립한다.
- **소품**: 폼 윈드스크린 달린 **핸드헬드 스틱 마이크**(가슴 높이).
- **프레이밍**: 현장 담당이므로 흉상이 아니라 **머리~허벅지 반신, 스탠딩**.
  **자세는 정면 직립** — 어깨·골반 수평, 고개 기울임 없음, 양발 균등, 마이크 든 팔 외
  나머지 팔은 자연스럽게 내림. 짝다리·손 허리 포즈는 쓰지 않는다(리그 파츠 분해 시 좌우 대칭 필요).
- **소품 선 최소화**: 마이크 케이블·랜야드 줄이 몸을 가로지르면 레이어 분리가 어려워진다.
  **무선 마이크 + 짧은 랜야드**로 선이 겹치지 않게 한다.
- **말투**: 낮고 안정된 톤, 짧고 단정한 문장, 결론부터.
- **상태**: 초안 `public/host/draft/rei.png` (= rei-v7-a, 정면 직립).

### 레이 제작 노트

- **눈 크기 문제와 그 원인**: 초기 시안들이 계속 눈이 작고 25세 이상으로 나왔다. 프롬프트를
  아무리 고쳐도 반영되지 않았는데, 진짜 원인은 프롬프트가 아니라 **codex가 참조 이미지를 아예
  읽지 못하고 있었던 것**이었다(아래 항목 참조). 참조 경로를 고친 뒤에는 얼굴 고정이 정상 작동한다.
- **머리 길이가 길어지면 얼굴이 늙는 편향**: 길이만 지시하면 모델이 얼굴을 성숙하게 그린다.
  → **얼굴이 잘 나온 시안을 참조로 박고 "길이만 바꾼다"고 지시**해야 한다.
- **길이가 계속 짧아지는 문제**: "어깨 길이"만으로는 턱선까지만 그린다.
  → **너무 짧은 시안을 비교 참조로 함께 넣고 "이것보다 눈에 띄게 길게"** 라고 지시하면 통한다.

### ⚠ codex-imagegen 함정 (이 프로젝트에서 실측)

**참조 이미지를 읽지 못하면서 조용히 실패한다.** Windows 샌드박스가 거부하면
(`failed to prepare windows sandbox wrapper: ... refusing to run unsandboxed`)
codex는 **참조 없이 그냥 생성해버린다** — 지시가 무시된 것처럼 보이지만 실제로는 참조가 없었던 것이다.
실측으로 두 경우에 발생했다:

1. `.tmp/` 같은 **숨김 디렉터리**의 이미지를 참조로 넘길 때
2. 참조와 출력의 **루트가 다를 때**(참조 `generated_images/` + 출력 `gen-out/`) — 간헐적

→ **참조와 출력을 같은 디렉터리로 맞춘다**(예: 둘 다 `gen-out/`). 숨김 경로는 쓰지 않는다.

**입력 이미지를 편집하는 모드가 없다.** i2i/인페인팅을 시켜도 `EDIT MODE UNAVAILABLE`이다.
"이 이미지의 이것만 고쳐라"는 생성으로 풀 수 없다 → §6 참조.

## 3. 카이 (Kai) ♂ — 데이터·분석

**한 줄**: 패치노트·지표를 뜯어보고 낮은 톤으로 결론을 놓는 브레인.

한때 보류했다가(여성 캐릭터로 방향 전환) 재개해 확정했다.

- **머리**: 딥 네이비(`#2a3a6b`) **단일색** — 투톤·이너하이라이트·그라데이션 금지.
  짧고 정돈된 레이어컷 + 살짝 긴 앞머리.
- **눈**: 앰버. 하이라이트 있는 광택 홍채. **아래 속눈썹 없음**(위 눈꺼풀 선만).
- **얼굴**: 20대 초중반, 짧은 얼굴·찬 볼·가는 콧대의 **미형(비쇼넨)**. **볼터치 없음** —
  피부는 얼굴 전체가 균일한 톤. 명암 대비 낮은 **소프트 애니 채색**으로 로지·미아에 맞춘다.
- **복장**: 화이트 셔츠 **소매 롤업** + **코발트 블루 슬림 타이**(시그니처) +
  다크 차콜 슬림 슬랙스 + 얇은 검정 벨트. 무테 안경.
  **베스트·니트·재킷 등 셔츠 위 레이어는 쓰지 않는다**(리그 파츠에서 팔 분리가 어려워진다).
- **소품**: **무선 핸드헬드 스틱 마이크**(가슴 높이, 타이를 가리지 않게 중앙에서 비껴) +
  내린 손에 슬림 태블릿(허벅지 옆). 팔이 몸을 따라 곧게 내려가므로 "한 팔은 내림" 규칙과 양립한다.
- **프레이밍**: 미아·레이와 동일 규격 — **머리~허벅지 반신, 정면 직립**(§5).
- **말투**: 낮은 톤, 짧고 단정한 문장, 결론부터. 농담은 건조하게 한 번.
- **상태**: 초안 `public/host/draft/kai.png` (= kai-half4-b + 얼굴 리터치)

### 카이 제작 노트 (남성 캐릭터의 함정)

1. **프롬프트를 방치하면 세이넨 극화체로 흐른다** — "큰 눈·둥근 턱·20대 초반"을 매번 명시해야 한다.
2. **"샤프하게"를 이목구비에 걸면 안 된다** — 연령이 30대로 올라가고 눈매가 사나워진다.
   샤프는 **선과 디테일**에만 걸 것.
3. **원색 파란 머리는 위화감이 크다** — 머리색을 시그니처로 쓰는 규칙은 여성 캐릭터에만 적용하고,
   남성은 저채도 머리 + 넥웨어로 시그니처를 준다.
4. **"중간으로 섞어라"는 지시는 축을 명시해야 한다** — 나이대·그림체·이목구비·소품이 각각 다른 축이다.
   방향이 불확실하면 **중간값을 사이에 두고 양쪽으로 브래킷 생성**하면 한 번에 좁혀진다
   (어리게/나이 들게 두 장을 동시에 뽑아 고르게 하는 방식).
5. ⚠**참조 이미지의 특징을 상대 표현으로 지시하면 그 방향으로 더 밀린다.**
   "B의 얼굴로" 하려고 "더 긴 얼굴·더 좁은 눈"이라고 썼더니 모델이 그걸 기준점이 아니라
   **방향**으로 읽어 B보다 더 날카롭고 더 나이 든 결과를 냈다.
   → **절대 묘사로 쓴다**("중간 크기 아몬드 눈, 둥근 턱"). 여기에 **"참조보다 더 ~하게 가지 말 것"**을
   금지로 붙이고, **실패본을 부정 참조로 함께 넣는다**("이것처럼 나오면 틀린 것"). 이렇게 하니 잡혔다.
6. **미형화와 어리게 만들기는 같은 방향이 아니다.** 둘을 한 번에 걸면 "성숙한 미남" 쪽으로
   흡수돼 연령이 안 내려간다(20세 요청 → 23~25세로 나옴). 어림을 우선순위로 명시해야 한다.

---

## 4. 확정 시 필요한 후속 작업

- [ ] 이름 확정 (미아 / 레이 / 카이)
- [x] 레이 헤어 길이 확정 — 어깨선 레이어드 중단발
- [x] 레이 자세 확정 — 정면 직립 (rei-v7-a)
- [x] 미아 프레이밍을 레이와 동일한 반신 정면 직립으로 통일 (mia-v2-worn)
- [x] **리포터 공용 캔버스 규격 확정 + 베이스 생성** → 아래 §5
- [x] 카이 재개·확정 — 네이비 단일 헤어, 베스트 없음, 미형 (kai-half4-b)
- [x] 카이 프레이밍을 미아·레이와 같은 반신 정면 직립으로 통일 + §5 규격 적용
- [x] 카이 마이크/타이 겹침 해소 (마이크를 중앙에서 비껴 잡게 함)
- [x] 카이 아래 속눈썹·볼터치 제거 → §6 리터치 도구
- [ ] 리그 파츠 세트 (`HOST-RIG-PARTS.md` 규격: blink / expr 8종 / mouth 7종) — 이때 레이 눈 크기 보정
- [ ] `src/components/HostStage.astro` 다중 호스트 지원 (현재 로지 하드코딩)
- [ ] `content.config.ts` frontmatter에 `host: rosie | mia | rei | kai` 필드 추가
- [ ] 음성 프리셋 (`roji-tts` 스킬 확장 — 미아=하이톤 빠름, 레이=로우톤 느림, 카이=남성 로우톤)

> 초안 이미지: `public/host/draft/mia.png`, `rei.png`, `kai.png`

---

## 5. 리포터 공용 캔버스 규격

씬 단위로 캐릭터를 바꿔 끼우려면 리포터들의 **눈높이와 인물 크기가 같아야** 한다.
안 맞으면 씬이 넘어갈 때 인물이 위아래로 튀거나 커졌다 작아진다. 대상은 미아·레이·카이 3인이다.

### 규격

| 항목 | 값 | 비고 |
|---|---|---|
| 캔버스 | **900 × 1350** (2:3) | 배경 투명(알파) |
| 광대선 y | **23.5%** (317px) | 눈높이 기준점 |
| 얼굴 광대폭 | **22.5%** (203px) | 인물 스케일 기준 |
| 얼굴 중심 x | **50%** | |
| 하단 | 허벅지 중간에서 프레임 아웃 | 여백 없이 채운다 |
| 정수리 여백 | **고정하지 않음** | 헤어 볼륨만큼 캐릭터별로 다르다 |

**왜 얼굴을 기준으로 잡는가.** 처음엔 "정수리 여백 + 머리끝~어깨 길이"로 잡으려 했으나
헤어 볼륨에 완전히 휘둘렸다 — 어깨행 자동 검출이 미아 54%/레이 미검출로 실패했고,
머리 최대폭도 미아 33.8% vs 레이 43.0%로 벌어졌다. 반면 **광대폭은 헤어와 무관**하고
**광대선은 눈높이에 바로 대응**한다. 그래서 정수리 여백은 결과값으로 두고 얼굴을 고정한다
(미아 1.8% / 레이 4.2% — 미아 포니테일이 더 높아서 생기는 정상적인 차이).

**수치 근거.** 승인 시안 실측이 광대선 21.3%/20.1%, 얼굴폭 21.5%/22.5%였다.
여기서 크게 벗어나면 인물이 작아져 하단에 흰 여백이 생기므로 근처 값으로 정했다.
`FACE_W=0.225`는 두 인물 모두 허벅지 크롭이 하단을 넘겨 채우는 값이고,
`CHEEK_Y=0.235`는 머리가 가장 높은 미아도 정수리가 잘리지 않는 값이다.

### 파이프라인

```bash
# 베이스 생성 — 규격 정렬 + 배경 알파 컷을 한 번에
node tools/host-base-align.mjs --in public/host/draft/mia.png --out public/host/base/mia.png
node tools/host-base-align.mjs --in public/host/draft/rei.png --out public/host/base/rei.png
node tools/host-base-align.mjs --in public/host/draft/kai.png --out public/host/base/kai.png

# 검출 확인 (빨강=광대선, 초록=얼굴폭, 파랑=정수리)
node tools/host-base-align.mjs --diag public/host/draft/rei.png /tmp/rei-diag.png

# 정렬 검증 시트 (눈높이가 한 선에 오는지 눈으로 확인)
node tools/host-base-align.mjs --sheet public/host/base/mia.png public/host/base/rei.png public/host/base/kai.png /tmp/sheet.png
```

`--no-alpha`로 알파 컷을 끄고 흰 배경 정렬본만 얻을 수 있다. `--alpha <src> <dst>`는 알파 컷만.

- `public/host/draft/` = **승인된 원본 아트**(생성 결과 그대로)
- `public/host/base/` = **규격 정렬 + 알파 컷 결과**(draft에서 도구로 파생, 손으로 고치지 않는다)
- 규격은 생성 프롬프트로 지킬 수 없다(모델이 픽셀 위치를 못 맞춘다). 반드시 후처리로 강제한다.
- 아트를 다시 승인받으면 `draft/`만 갈아끼우고 위 명령을 다시 돌리면 된다.

### 배경 알파 컷 방식

**색으로 흰색을 일괄 제거하면 안 된다** — 두 캐릭터 모두 화이트 셔츠라 셔츠가 통째로 날아간다.
"프레임 가장자리와 연결된 밝은 영역"이라는 **위치 조건**이 필요하고, 그건 플러드필로만 판정된다.
셔츠는 어두운 선화로 둘러싸여 바깥과 끊겨 있으므로 살아남는다.

- 임계값 2개(히스테리시스): `FILL=244`(배경으로 보고 전파) / `EDGE=224`(사이 구간은 부분 알파)
- ⚠**흰 테두리(할로) 문제**: 원본 배경이 순백이 아니라 아주 연한 회색이어서, 경계
  안티에일리어싱 픽셀이 `FILL` 아래로 살아남아 실루엣에 흰 테두리가 남는다.
  → `RIM=232` 이상인 "배경에 닿은 밝은 픽셀"을 **2겹 침식**해서 제거한다.
- 배경 흰색과 셔츠 흰색은 색으로 구분되지 않으므로 도구는 자동 판정하지 않는다.
  팔·몸통 사이가 뚫렸는지는 **결과를 색 배경(마젠타 등)에 올려 눈으로 확인**한다.

### 남은 것

- 로지는 바스트업(500×500)이라 이 규격에 포함되지 않는다. 앵커 컷과 리포터 컷은 별도 규격이다.
- 리그 파츠 분해 시 이 베이스를 원본으로 쓴다(알파가 이미 잡혀 있어 레이어 분리에 바로 들어갈 수 있다).

---

## 6. 얼굴 디테일 리터치 (`tools/host-face-retouch.mjs`)

**"승인된 그림에서 이 디테일만 빼라"는 요구는 재생성으로 풀면 안 된다.**
생성 모델은 매번 전체를 다시 그리므로 요청하지 않은 것까지 바뀐다. 실측:
프레임 비율이 2:3 → 1:2로 홀쭉해지고, 마이크 든 손이 반대쪽으로 뒤집혔다.
codex의 `image_gen`은 입력 이미지를 받는 편집 모드가 없다(`EDIT MODE UNAVAILABLE`).

→ 승인본 픽셀을 유지하고 **해당 영역만 프로그램으로 고친다.**

```bash
# 검출 범위 확인 (초록=홍채bbox, 빨강=속눈썹 밴드, 하늘=볼터치 밴드)
node tools/host-face-retouch.mjs --diag public/host/draft/kai.png /tmp/kai-diag.png

# 적용 (둘 다 기본 실행 / --no-lash, --no-blush 로 개별 해제)
node tools/host-face-retouch.mjs --in <src> --out <dst>
```

### 검출 방식과 그 근거

- **눈 위치는 홍채 색으로 찾는다.** 실측 홍채는 rgb(221,146,66)·(243,173,89) 계열로 `r-b`가 140~155.
  그늘진 피부는 `r-b`가 50~80이라 **`r-b>115`로 확실히 갈린다.**
  느슨하게 잡으면 볼 그늘을 홍채로 오인해 눈 bbox가 118×494px까지 부풀었다.
- **좌우 눈은 x 중앙값이 아니라 "x 분포의 최대 빈 구간"에서 가른다.** 중앙값으로 자르면
  두 홍채가 한쪽 군집에 몰려 들어간다(실측: A가 104px 폭).
- **볼터치 범위는 눈 높이 배수로 추정하면 안 된다.** 홍채는 밝은 부분만 검출되므로 배수식이
  너무 짧게 잡혔다(추정 y248~273 vs 실제 y244~296). **행별 "붉기 초과" 픽셀 수 프로파일**을 만들어
  피크의 25% 아래로 떨어질 때까지 넓힌다. 아래로는 입술(y324~)·목 그늘(y340~)이 있지만
  그 사이 붉기가 거의 0으로 끊기므로 자연히 입술 앞에서 멈춘다.

### 리터치 방식

- **볼터치 제거**: 이마에서 기본 피부색조를 샘플(중앙값)해, 밴드 안 피부 픽셀의 색조를 그 값으로
  정규화한다. G는 그대로 두고 R·B만 옮기므로 **음영 구조가 보존된다.** 밴드 경계는 페더 처리.
  붉기 초과가 큰 픽셀(입술 등)은 임계값으로 보호한다.
- **아래 속눈썹 제거**: 대상 픽셀을 **주변 반경 4px의 피부색 평균**으로 덮는다.
  ⚠같은 열의 단일 피부색을 복사하면 **세로 얼룩이 생기고 홍채 아랫부분까지 잘린다**(1차 시도 실패).
  ⚠밴드를 눈 전체 폭으로 잡으면 아래 눈꺼풀 선까지 지워 눈이 뭉개진다
  → **각 눈의 바깥쪽 절반만** 대상으로 한다(아래 속눈썹은 눈꼬리 쪽 짧은 사선 몇 개다).

### 검증

확대 이미지로만 판단하지 말 것. 3~4배 확대에서는 잔흔이 크게 보이지만 **1:1 실제 크기**에서는
보이지 않는다. 확대 비교로 눈 손상 여부를 보고, 1:1 비교로 최종 수용 여부를 판단한다.
